<div class="card">
  <div class="card-body">
  <h3>My Bookings</h3>
  <% @bookings.each do |booking| %>
    <strong><%= booking.car.title %></strong> <%= link_to "View booking", booking_path(booking) %>
    <p>Booked for: <%= booking.start_date %> - <%= booking.end_date %> </p>
  <% end %>
  </div>
</div>
